torch_batch: 1
num_workers: 12
rate_of_validation: 5 # how often the validation is done in epochs
prefetch_factor: 2 # 2 is the default value, do not change this change torch_batch instead
sample_stepsize: 4 # Step_size of the sampling for the validation set