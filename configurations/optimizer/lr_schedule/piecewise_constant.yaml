type: "piecewise_constant"
init_learning_rate: 0.005
boundaries: [500, 1000]  # Boundaries at which to multiply the learning rate by the corresponding scale
scales: [0.1, 0.1]  # Multiplication factor at each boundary
