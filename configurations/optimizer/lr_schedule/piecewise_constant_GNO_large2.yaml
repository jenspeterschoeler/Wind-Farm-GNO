type: "piecewise_constant"
init_learning_rate: .1e-1
boundaries: [75, 150] # Boundaries at which to multiply the learning rate by the corresponding scale
scales: [.1, .1] # Multiplication factor at each boundary
